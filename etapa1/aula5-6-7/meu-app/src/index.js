
import ReactDOM  from "react-dom";
import App from './app'

ReactDOM.render(
   // <App text='alex' name='props name' children={'children'}/>,
   <App>children</App>,
   document.querySelector('#root')
)